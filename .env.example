# =============================================================================
# Frontend environment template.
# Copy to .env.local and fill in real values. Never commit .env.local.
# =============================================================================

# --- Supabase (publishable values, safe to expose to the browser) -------
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Backend base URL (Spring Boot) -------------------------------------
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Email / SMTP intentionally omitted - paused, will be added back in a later PR.
